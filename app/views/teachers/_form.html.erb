<%= form_for(@teacher) do |f| %>
  <% if @teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@teacher.errors.count, "error") %> prohibited this teacher from being saved:</h2>

      <ul>
      <% @teacher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.text_field :name, placeholder: "Teacher Name", class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.number_field :grade_level, placeholder: "Grade Level Taught", class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.number_field :room, placeholder: "Room #", class: "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
